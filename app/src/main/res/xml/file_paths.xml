<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only explicitly shared items are exposed, and only via temporary URI grants. -->
    <files-path name="timelapses" path="timelapses/" />
    <cache-path name="shared" path="shared/" />
</paths>
